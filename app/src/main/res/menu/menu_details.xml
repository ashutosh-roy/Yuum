<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/Details"
        android:title="Hotel info"
        />
    <item android:id="@+id/Color"
        android:title="Change backgound color"/>
    <item android:id="@+id/SignOut"
        android:title="Sign Out"
        />
    <item android:id="@+id/CheckOut"
        android:title="Check Out"
        app:showAsAction="always"
        android:icon="@mipmap/ic_checkout_round"
        />
    <item  android:id="@+id/Close"
        android:title="Close"
        app:showAsAction="always"
        android:icon="@mipmap/ic_close"/>
</menu>